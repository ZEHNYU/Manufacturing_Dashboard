MP_Tag = IF('R_WIP_TRACKING_T'[PO_NO]="MP", "MP","QT")

Target SN Distinct Count = 
CALCULATE(
    VALUE(DISTINCTCOUNT('R_WIP_TRACKING_T'[SERIAL_NUMBER])),
     'R_WIP_TRACKING_T'[GROUP_NAME]<> "0"
)